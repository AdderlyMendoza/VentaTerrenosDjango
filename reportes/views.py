from django.shortcuts import render

# Create your views here.
from django.shortcuts import render

# Create your views here.
def app_reportes(request):
    return render(request, 'reportes/app_reportes.html')